image:
  file: .gitpod.Dockerfile

tasks:
  - init: |
      composer install
      npm install
      npm run dev
    command: php artisan serve --host=0.0.0.0 --port=8000

ports:
  - port: 8000
    onOpen: open-preview
